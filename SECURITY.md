# セキュリティポリシー — StampRallyBot

このドキュメントは実装・運用上のセキュリティ方針をまとめたもの。実装レベルの詳細ルールは [CLAUDE.md](CLAUDE.md) / [AGENTS.md](AGENTS.md)、非機能要件は [docs/requirements.md](docs/requirements.md) を参照。

## 認証・認可

- 管理者はCookieベースのセッション認証でログインする
- セッションCookie（`tower-sessions`）は `HttpOnly` / `Secure` / `SameSite=Strict` を有効にする（ライブラリの既定値をそのまま使用し、明示的な無効化はしない）。JavaScriptからの読み取り・非HTTPS経路での送信・クロスサイトでの自動送信をいずれも防ぐ
- ログイン成功時にセッションIDをローテーションする（`Session::cycle_id()`。session fixation対策）
- パスワードは Argon2 でハッシュ化し、平文では保存しない
- 管理者ログインは**ユーザー名を持たない単一パスワード認証**であるため、総当たりを防ぐ試行回数制限を設ける。同一送信元（`X-Forwarded-For` の最右値）からの失敗が5回に達した時点で15分間ブロックし、ブロック中はパスワード照合を行わずに `429` を返す。送信元を問わない全体上限は、第三者が正規の管理者を締め出せてしまうため採用しない（詳細は[docs/architecture.md 25節](docs/architecture.md#25-管理者ログインのレート制限post-authlogin)）
- プレイヤーはLINEアカウントのみで識別し、Webログインは行わない
- 管理画面（`/admin/*`）はすべてセッション認証ミドルウェアを通す

## CSRF対策

- すべてのPOSTフォームにCSRFトークンを付与する
- 例外: LINE Webhook `/callback` とLIFFチェックイン `/liff/checkin` はLINE/LIFFからの直接呼び出しのため、CSRF保護の対象外とする

## LINE Webhookの署名検証

- `/callback` は `x-line-signature` ヘッダー（チャネルシークレットによるリクエストボディのHMAC-SHA256をBase64エンコードした値）を検証してから処理する
- ヘッダー欠如・検証失敗はいずれも `401 Unauthorized` とし、以降のJSONパース・業務ロジック（`game_service`）を一切実行しない（なりすまし・改ざんされたWebhookリクエストの遮断）
- 検証は生のリクエストボディ（バイト列）に対して行う。JSONへデシリアライズ後に再シリアライズした値を使わない（元のボディとバイト単位で一致する保証がないため）
- 詳細: [docs/architecture.md](docs/architecture.md) 8節

## QRチェックインの検証

QRコード自体は各部屋の担当スタッフが目視判断で提示する運用のため、「QR画像の使い回し」自体はシステムでは積極的に防止せず、運用でカバーする方針としている。ただしサーバ側では、チェックインのたびに必ず以下を検証する。

- 送信されたUUIDが有効な部屋のものか
- そのプレイヤーの `current_room_id` と一致するか（案内されていない部屋のQRは無効）
- 判定モードが「QR＋正解入力」のイベントでは、`answer_verified` が true であることを確認する

クライアント（LIFF側）からの申告を信用せず、必ずサーバ側で再検証する。

## LIFFのLINEユーザーID検証（なりすまし対策）

- `POST /liff/checkin` はLINEアカウントで参加者を識別するが、クライアント（ブラウザ上のJavaScript）が送ってきた「LINEユーザーID」の文字列をそのまま信用しない（任意の文字列を送るだけで他人になりすませてしまうため）
- 代わりに `liff.getIDToken()` で取得したIDトークン（JWT）をLINEの検証エンドポイント（`POST https://api.line.me/oauth2/v2.1/verify`）に問い合わせ、有効な署名・有効期限・対象チャネル（`client_id` = `LINE_LOGIN_CHANNEL_ID`）であることを確認した上で、その応答に含まれる `sub` をLINEユーザーIDとして採用する
- 検証に失敗した場合（期限切れ・改ざん・対象チャネル不一致等）は401とし、以降のチェックイン処理を一切行わない
- 詳細: [docs/architecture.md](docs/architecture.md) 15節

## シークレット管理

- LINEチャネルシークレット・アクセストークン、DB接続情報、管理者パスワード等はすべて環境変数（`.env`）経由で注入する
- `.env` はコミットしない（`.gitignore` 済み）。値のテンプレートは [.env.example](.env.example) を参照する
- devcontainerの `compose.yaml` もハードコードを避け、`env_file` 経由で `.env` を参照する構成にしている

## 本番DB（TiDB Serverless）の接続方針

本番DBは同一TiDB Cloudアカウント内の既存クラスタを、別アプリと共用する（クラスタ内でデータベースのみ分ける）。以下を必須の分離策とする。

- **専用データベース**: このアプリ専用のデータベース（例 `stamprally`）を作成し、既存アプリのデータベースとはテーブル名空間を分離する
- **最小権限の専用DBユーザー**: `DATABASE_URL` には、上記の専用データベースに対してのみ権限を持つDBユーザーを使う。既存アプリと共通の管理者/rootユーザーを使い回さない。これにより、このアプリ側の資格情報が万一漏洩した場合や、このアプリ側にSQLインジェクション等の脆弱性が仮にあった場合でも、被害を自身のデータベース内に閉じ込める
- **TLS必須接続**: TiDB Serverlessへの接続は暗号化必須とする。本番の`DATABASE_URL`がTLS検証込みの接続文字列になっていることをデプロイ時に確認する（盗聴・改ざん対策）

### 受容リスク: パブリックエンドポイントであること

TiDB Serverless（無料枠）はVPCピアリング等のプライベートネットワーク接続を提供せず、接続はパブリックインターネット経由になる。このため「ネットワーク経路を知らない第三者は原理的に到達できない」という防御層は存在せず、安全性は実質的に**資格情報の強度とTLSのみ**に依存する。

- ホスティング先（Renderの無料枠）は基本的に固定送信元IPを提供しないため、TiDB側でのIPアローリストは実用的な対策として採用できない前提で設計する
- 補完策として、専用データベース・最小権限ユーザー・TLS必須化（上記）に加え、DBユーザーのパスワードは十分な長さのランダム値を発行し、使い回さない
- 扱うデータ（LINEユーザーID、参加者名・チーム名、部屋の訪問記録・タイムスタンプ）に決済情報等の高感度データは含まれないことを踏まえ、上記の補完策を講じた上でこのリスクを受容する。資格情報の漏洩が疑われる場合は、直ちにDBユーザーのパスワードをローテーションする

## 画像アップロード

- 拡張子だけでなく、マジックバイト検証で実際のファイル形式を確認する
- サイズ上限を設ける

## 依存関係・再現性

- 依存クレートのバージョンは `Cargo.lock` で固定する
- devcontainerのCLIツール（`sqlx-cli`）のバージョンは `.devcontainer/Dockerfile` で固定する。ベースイメージ（`mcr.microsoft.com/devcontainers/rust:1-bookworm`）はメジャーバージョンのみを指定した可動タグで、パッチ更新のたびに中身が変わりうる（完全固定タグ`1.85-bookworm`等は存在しないため採用していない。経緯は`instructions/done/devcontainer-hardening.md`参照）。ローカル開発環境のみに影響し、本番ビルド（リポジトリルートの`Dockerfile`）には無関係

## 脆弱性を発見した場合

このプロジェクトは非公開の個人開発プロジェクト。脆弱性や懸念点を発見した場合は、開発者（gantaro）に直接連絡すること。

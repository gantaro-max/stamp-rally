# AGENTS.md — StampRallyBot（仮称）Codex ガイド

## Codexの役割定義

Codex はこのプロジェクトにおいて **コーダー・テスター・一次レビューリーダーの役割** を担う。

### 担当する業務

| 業務 | 内容 | 成果物 |
|:--|:--|:--|
| **実装** | Claude が作成した実装指示書をもとに、TDD（Red-Green-Refactor）でコードを実装する | ソースコード・テストコード |
| **テスト** | 実装前に失敗するテストを書き、実装後にすべて通ることを確認する | テスト結果・テストコード |
| **一次レビュー（リーダー）** | 複数のサブエージェントを並列起動し、実装コードの一次レビューをとりまとめる | レビューレポート |

### やらないこと

- 要件定義・基本設計は行わない。設計上の疑問は実装前に Claude へフィードバックする。
- ドキュメント（設計書・`CHANGELOG.md` 等）の更新は行わない。ドキュメント更新は Claude が担当する。
- 実装指示書にない範囲の機能追加・リファクタリングは勝手に行わない。

---

## 開発ワークフロー（Codex視点）

```
1. Claude から実装指示書を受け取る
        ↓
2. `main` から `feature/<機能名>` ブランチを作成する（下記「ブランチ運用」参照）
        ↓
3. 実装指示書と設計書（docs/requirements.md / docs/architecture.md / docs/api.md / docs/database.md）を読み、不明点があれば着手前に確認する
        ↓
4. 指示書の「テストケース」ごとにTDDサイクルを回す（下記「TDD（Red-Green-Refactor）」参照）。各ステップごとにコミットを分ける
   4-1. Red:      失敗するテストを書き、実際に失敗することを確認してコミットする
   4-2. Green:    テストを通す最小限の実装を書いてコミットする
   4-3. Refactor: テストが通ったままコードを整理してコミットする（必要な場合のみ）
        ↓
5. すべてのテストケースについて 4 を繰り返し、`cargo test` が全体で通ることを確認する
        ↓
6. 一次レビュー（セルフチェック）を実施する（後述、TDDサイクルが守られているかも確認する）
        ↓
7. featureブランチをpushし、Pull Requestを作成する（`gh pr create`）
        ↓
8. レビュー結果とPRのURLを Claude へ報告する
```

---

## ブランチ運用 — 必須

- `main` への直接コミット・直接pushは禁止。作業は必ず `main` から作成した `feature/<機能名>` ブランチで行う
  - ブランチ名は対応する実装指示書のファイル名と揃える（例: `instructions/initial-setup.md` → `feature/initial-setup`。実装完了・マージ済みの指示書は `instructions/done/` 配下に移動するが、ブランチ名との対応はファイル名（拡張子を除く部分）のみで決まり、フォルダの位置は関係ない）
- 実装が完了し、TDDサイクルと一次レビューを終えたらブランチをpushし、`main` 宛にPull Requestを作成する
- マージは **Squash merge** で行う（ブランチ内のRed/Green/Refactorの細かいコミットは、`main` 上では1つの変更にまとめる）
- マージ後、featureブランチは削除する
- 最終的なマージ判断はClaudeの最終レビュー通過後に行う（[CLAUDE.md](CLAUDE.md) 参照）

---

## 一次レビューの進め方

**観測事実**: サブエージェントを並列起動する一次レビューは、`agent thread limit reached`、サンドボックス初期化時の `bwrap` 関連エラー、無応答タイムアウト（`close_agent` 時点で `previous_status: running`、すなわち完了すらしていない状態）を繰り返し起こしている。これまでに次の対策を講じたが、いずれも再発を防げなかった。

- 同時起動数を4→2本に制限する（`aaa0e6a`）
- 完了したサブエージェントは受領直後に必ず `close_agent` を呼ぶ運用を明記する（`fb06641`。当時は[openai/codex#22779](https://github.com/openai/codex/issues/22779)の「未クローズのエージェントがスレッド枠を解放しない」バグが原因と判断したが、後に`previous_status: running`（エージェントが完了すらしていない）という別症状が観測されたため、この診断だけでは説明がつかないことが分かった）

そのため原因はまだ**確定していない**。関連する候補（未確定・いずれか一つに断定しない）:

- [openai/codex#22779](https://github.com/openai/codex/issues/22779) / [#23219](https://github.com/openai/codex/issues/23219) のような、Codex CLI側のエージェント枠管理の既知バグ
- サブエージェントがFSサンドボックス探索（`bwrap`）中にハングしている可能性（`previous_status: running`という観測はこの候補とも整合する）

**暫定方針**: 原因が特定できておらず対策のたびに再発している以上、これ以上このプロセスにチューニングコストを払わない。エージェントを一切起動せず、Codex自身が実装完了直後にその場で以下の観点をセルフチェックする方式に**切り替える**。

| 観点 | 確認内容 |
|:--|:--|
| セキュリティ | 認証・認可・CSRF・不正なチェックイン・入力検証・XSS の抜け漏れ |
| ロジック | 実装指示書の完了条件を満たしているか、ロジックの正確性 |
| コード品質 | 既存の設計パターンとの整合性、可読性、不要なコードの混入 |
| テスト | テストケースの網羅性、エッジケースの検証、Red-Green-Refactorが実際に踏まれているか（実装前に意味のある失敗をしたテストか） |

- `sqlx::test` 系のDB接続エラー（`docker`/`gh`/DBホスト名解決がこの開発環境に無いことに起因するもの）は既知の環境制約であり、セルフチェックの対象外である旨を報告に明記する
- このセルフチェックは完了必須の関門ではない。最終的な合否判断はClaudeの最終レビュー（PR差分に対する複数サブエージェントでの並列レビュー）で行う
- セルフチェックで問題を見つけた場合は自己修正してから Claude へ渡す

### （将来の実験候補・未採用）サブエージェント安定化の対策案

FSサンドボックス探索によるハング候補が正しい場合の**対策案**（未採用・任意実験扱い）:

- 親CodexがGit diff・指示書該当箇所・テスト結果・既知の環境制約を事前に収集し「レビュー用パケット」としてサブエージェントのプロンプトに丸ごと渡し、追加のファイル探索・テスト実行はさせない
- 本レビュー前に疎通確認エージェントを1本起動し、60秒以内に応答しなければサブエージェントレビュー自体を実施しない
- 各ペア最大90〜120秒でタイムアウトし、無応答なら即 `close_agent` して親Codexによる構造化セルフレビューに切り替える

一次レビューが失敗するたびに異なる原因候補と対策案が示されており、これまでの2件（同時起動数の制限、`close_agent`運用の明記）はいずれも再発を防げなかった。そのため**この対策案も現時点では正式プロセスに採用せず、上記のセルフチェック方式を継続する**。Codexが手元の判断でこの方式を任意に試すことは妨げないが、複数回の実運用で安定して動作することが観測されるまでは、本節の必須プロセスには昇格させない。

---

## コーディング規約

### TDD（Red-Green-Refactor）— 必須

このプロジェクトでは **すべての機能実装をTDDで行う**。実装指示書の「テストケース」に挙げられた項目ごとに、以下のサイクルを厳格に守ること。

1. **Red**: 対象の振る舞いを検証するテストを先に書き、実装がまだ無い（または不十分な）状態で実際に失敗させる
2. **Green**: そのテストを通すための最小限の実装を書く。他のテストケースへの対応やリファクタリングは同時に行わない
3. **Refactor**: テストが通ったままの状態を保ちつつ、コードを整理する。新しいテストケースの追加はRefactorに含めない（それは次のRed）

**各ステップは必ず個別のコミットにすること**（例: `test: add failing test for GET /health`, `feat: implement GET /health`, `refactor: ...`）。featureブランチをPRとして提出する時点で、Red/Green/Refactorの過程がコミット履歴から追跡できる状態になっていること。1コミットに複数ステップをまとめない（`main`へのマージ時にSquash mergeでまとめられるのは、このブランチ内の細かいコミットであり、ブランチ上で最初から1コミットにしてよいという意味ではない）。

- 実装対象に「テストケース」の記載がない場合（Cargo.tomlへの依存追加やマイグレーションDDLなど、振る舞いを持たない変更）はTDDサイクルの対象外でよい。ただし、その変更によって新たに振る舞い（ハンドラー・サービス関数など）が生まれる場合は、その振る舞いに対してテストケースが必要かを自己判断し、必要であればClaudeに確認する
- テストの配置:
  - 単一モジュール・単一ハンドラー単位のテスト（ロジック単体、簡単なハンドラーの疎通確認等）: 対象モジュール内に `#[cfg(test)] mod tests` としてコロケートしてよい
  - DBに依存するテスト（`repository` 層など）: `sqlx::test` マクロを使い、マイグレーション適用済みのテスト用DBに対して検証する
  - 複数モジュール・複数ルートにまたがる結合テスト: `tests/` ディレクトリに配置し、Axumの `Router` 全体に対してリクエストを送る形で検証する
- 一次レビューでは、テストが実装より先に意味のある失敗をしていたかを（コミット順序やテスト内容から）確認する

### ハンドラー（Axum）
- 管理画面向けハンドラー（`/admin/*`）は必ずセッション認証ミドルウェアを通す
- 新しいエンドポイントを追加する場合、`docs/api.md` の設計と一致させる

### QRチェックイン（`/liff/checkin`）
- 受け取ったUUIDが有効な部屋のものか、プレイヤーの `current_room_id` と一致するかを必ずサーバ側で検証する
- 判定モードが「QR＋正解入力」のイベントでは、`answer_verified` がtrueであることも必ず検証してからチェックインを確定する

### テンプレート（Askama）
- すべての POST フォームに CSRF トークンを埋め込む
- フォームの `action` は設計書のパスと一致させる

### DBアクセス（sqlx）
- SQL は `src/repository/` 配下のモジュールにまとめる
- 生SQLをサービス層・ハンドラー層に直接書かない

### セキュリティ（実装時の必須チェック）
- パスワードは Argon2 でハッシュ化（平文保存禁止）
- ファイルアップロードはマジックバイト検証 + サイズ上限チェックを行う
- シークレットはコードに直接書かず、環境変数（`.env`、gitignore対象）で注入する
- LINE Webhook `/callback` と LIFFチェックイン `/liff/checkin` はCSRF除外が必須

---

## プロジェクト概要（クイックリファレンス）

- Rust（stable）/ Axum + Tokio / sqlx（生SQL） / Askama + Bootstrap 5
- Cookieベースのセッション認証（管理者ログインのみ）。プレイヤーはLINEアカウントで識別され、Webログイン不要
- LINE Webhook `/callback` と LIFFチェックイン `/liff/checkin` はCSRF除外
- 建物内最大15部屋・1イベント運用。参加者はランダムに割り当てられる部屋を巡り、QRコードをLIFFでスキャンしてチェックインする

### ハンドラー構成

| プレフィックス | モジュール | 役割 |
|:--|:--|:--|
| `/auth` | `handlers::auth` | 管理者ログイン・ログアウト |
| `/admin` | `handlers::admin` | 部屋管理・QRコード発行・設定・ランキング閲覧 |
| `/callback` | `handlers::line_webhook` | LINE Bot Webhook |
| `/liff/checkin` | `handlers::liff` | LIFFからのQRスキャン結果受信 |
| `/public` | `handlers::image` | 画像配信（認証不要） |

### 主要コマンド

```bash
cargo run                # アプリ起動
cargo build --release    # ビルド
cargo test                # テスト実行
docker-compose up -d     # ローカルDB起動
```

### 設計書・仕様書の参照先

| ドキュメント | 内容 |
|:--|:--|
| [docs/requirements.md](docs/requirements.md) | 要件定義 |
| [docs/architecture.md](docs/architecture.md) | アーキテクチャ・設計 |
| [docs/database.md](docs/database.md) | テーブル設計 |
| [docs/api.md](docs/api.md) | エンドポイント設計 |
| [SECURITY.md](SECURITY.md) | セキュリティポリシー |
| [CLAUDE.md](CLAUDE.md) | Claude（PM）の役割・ワークフロー |
